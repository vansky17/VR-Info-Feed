# Server-only credentials. Never prefix these with NEXT_PUBLIC_.
# Protects GET /api/ingest for a scheduler such as Vercel Cron.
CRON_SECRET=

# Optional persistence layer for the next milestone.
DATABASE_URL=

# Optional AI enrichment for cached summaries and relevance scoring.
OPENAI_API_KEY=
